version: '3'
services:
  server1:
    container_name: node-container-1
    build: 
      dockerfile: Dockerfile
    ports:
      - 5555:5000

networks: 
  default: 
      name: cpl-world
      external: true