version: '3'
services:
  server2:
    container_name: node-container-2
    build: 
      dockerfile: Dockerfile
    volumes:
      - .:/app
    ports:
      - 8888:8000

networks: 
  default: 
      name: cpl-world
      external: true